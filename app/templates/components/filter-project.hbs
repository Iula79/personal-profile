{{yield}}
Technologies: {{input value=filter key-up=(action 'autocomplete' filter)}}
<button {{action 'search'}}>Search</button>

<ul>
    {{#each filteredList as |item|}}
    <li{{action 'choose' item.technologies}}>{{item.technologies}}</li>
    {{/each}}
</ul>
